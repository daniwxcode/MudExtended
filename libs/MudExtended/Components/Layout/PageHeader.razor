@using MudBlazor

<div class="@CombinedClass">
    <PageTitle>@Title</PageTitle>
    
    <div class="d-flex justify-space-between align-center flex-wrap gap-4">
        <div>
            <MudText Typo="Typo.h4" Class="mudext-page-header__title">@Header</MudText>
            @if (!string.IsNullOrWhiteSpace(SubHeader))
            {
                <MudText Typo="Typo.body2" Class="mudext-page-header__subtitle">@SubHeader</MudText>
            }
        </div>
        
        @if (Actions is not null)
        {
            <div class="mudext-page-header__actions">
                @Actions
            </div>
        }
    </div>
    
    @if (ChildContent is not null)
    {
        <div class="mt-4">
            @ChildContent
        </div>
    }
</div>

@code {
    /// <summary>
    /// Titre de la page (balise title).
    /// </summary>
    [Parameter, EditorRequired]
    public string Title { get; set; } = string.Empty;

    /// <summary>
    /// Titre affich�.
    /// </summary>
    [Parameter, EditorRequired]
    public string Header { get; set; } = string.Empty;

    /// <summary>
    /// Sous-titre.
    /// </summary>
    [Parameter]
    public string? SubHeader { get; set; }

    /// <summary>
    /// Boutons d'actions.
    /// </summary>
    [Parameter]
    public RenderFragment? Actions { get; set; }

    /// <summary>
    /// Contenu additionnel.
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    /// <summary>
    /// Classes CSS additionnelles.
    /// </summary>
    [Parameter]
    public string? Class { get; set; }

    private string CombinedClass => string.IsNullOrWhiteSpace(Class)
        ? "mudext-page-header mb-4"
        : $"mudext-page-header mb-4 {Class}";
}
